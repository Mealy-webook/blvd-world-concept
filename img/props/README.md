# Floating hero props

Drop transparent PNGs here with these names and they replace the emoji placeholders automatically:

- prop-a.png  (top-left,  ~84px)
- prop-b.png  (top-right, ~110px)
- prop-c.png  (mid-left,  ~96px)
- prop-d.png  (mid-right, ~78px)
- prop-e.png  (bottom-left, ~70px)
- prop-f.png  (bottom-right, ~88px)

Position/size/glow color per slot is configured in js/scene.js (PROPS array).
